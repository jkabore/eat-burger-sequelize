<div class="wraper">
    <div class="jumbotron">
        <div class="container">
  <div class="row">
    <div class="col">
    <ul>   
      <li>
          {{#each burgers}}
           {{#unless devoured}}
           <div>
              <form>
			<input type="hidden" name="devoured" value="true">
		    <button class="btn btn-default btn-lg" id="bttn1" type="submit">Devour </button>
			</form>
            </div>
          {{/unless}}
        {{/each}}
      </li>
      </ul>
    </div>
    <div class="col">
<ul>
				{{#each burgers}}
					{{#if devoured}}
					<li>
						<p>{{burger_name}} </p>
					</li>
					{{/if}}
				{{/each}}
				</ul>
     
    </div>
  </div>
  </div>
    <div class="col align-self-center">
        <form class="create-update-form">

			<div class="form-group">
				<textarea class="form-control input-lg" type="text" name="burger_name" rows="3"> </textarea>
			</div>

			<button class="btn btn-default btn-lg" id="bttn2" type="submit">Add Burger!</button>
		</form>
      
    </div>
        
    </div>
</div>
</div>